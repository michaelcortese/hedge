# Secrets — never commit
config.yaml
# ...but the secrets-FREE deploy config IS committed (baked into the image).
!deploy/config.yaml
secrets/
*.pem
*.key

# Cached weather/market fetches and tournament outputs
data/cache/
data/runs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
build/
dist/
